autofix_from_log.py : If lookup of a missing type a permuted(for impropers)/reversed(for angles/dihedrals) token is searched, it creates a log for those entries.

fix_permuted_coeffs.py : From that log generated by autofix rearranges the parameters columns accordingly for class 2 convention (see lammps docs).
