autofix_from_log.py.1 : Copies every parameter terms (coefficients and equilibrium values) as is, eventhough during lookup of a missing type a permuted(for impropers)/reversed(for angles/dihedrals) token is searched.

autofix_from_log.py.2 : If lookup of a missing type a permuted(for impropers)/reversed(for angles/dihedrals) token is searched, it creates a log for those entries.

fix_permuted_coeffs.py : From that log generated by autofix rearranges the parameters columns accordingly for class 2 convention (see lammps docs).

submission_polymerizing.sh : slurm script and loop
